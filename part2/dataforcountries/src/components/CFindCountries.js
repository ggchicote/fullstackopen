import React from 'react'

const CFindCountries = ({value, onChange}) => {
    return(
        <>
            find countries <input value={value} onChange={onChange}/>
        </>
    )
}
export default CFindCountries